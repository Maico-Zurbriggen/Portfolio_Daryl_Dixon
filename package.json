{
  "name": "portfolio_daryl_dixon",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.3.0"
  },
  "engines": {
    "node": ">=17.0.0"
  },
  "devDependencies": {
    "vite": "^6.2.2"
  }
}
